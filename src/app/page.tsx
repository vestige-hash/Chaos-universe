import { Chat } from '@/components/chat';

// Next.js App Router requires default exports for pages
// eslint-disable-next-line import/no-default-export
export default function HomePage() {
  return <Chat />;
}
